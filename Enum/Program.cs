void Move(Direction[] directions)
{
    if (directions[0] == Direction.Down)
    {
        
    }
}

var Up = 1;
var Down = 2;
var Left = 3;
var Right = 4;

var x = Console.ReadLine() ?? "";

var hello = "hello";
var list = new List<string>(); // ["h", "e", "l", "l", "o"];

foreach (var letter in hello)
{
    list.Add(letter.ToString());
}



Move([Direction.Up, Direction.Up, Direction.Left, Direction.Up, Direction.Right]);

Console.BackgroundColor = ConsoleColor.Red;
Console.ForegroundColor  = ConsoleColor.Green;
Console.WriteLine(123);

enum Direction
{
    Up = 1,
    Down = 2,
    Left = 3,
    Right = 4
}

enum Sex
{
    Male = 0,
    Female = 1
}

