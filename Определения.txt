Свойство - private поле + 2 public метода (getField, setField)

Автосвойство - свойство с дефолтными реализациями методов get и set. Дефотлная реализация для get - вернуть значение поля, для set - установить значение в поле